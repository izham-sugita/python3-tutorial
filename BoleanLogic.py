#! /usr/bin/env python3
t = True
f = False
print(type(t)) #will print "<class 'bool'>"
print(t and f) # use English words 'and' rather than '&&'
print(t or f) # rather than '||'
print(not t) # rather than '/='
print(t != f) # logical XOR

